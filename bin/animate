#! /usr/bin/env python
# -*- coding : utf8 -*-

#from shs.calc import SiestaCalc
from calc import SiestaCalc
import plot as Plot 

def setup():
    template = '../template'
    c = SiestaCalc(template, mode='out', steps = range(-500, 0))
    c.animate()

#    c.geom.initialize('FCC', {'Fe':3, 'C':6}, [5,4,4], 2.5, 'Ang', DistLevel = 10.0)
#    c.geom.geom2opts()
#    c.ctype.alter('MD', temp = 200)
#    c.write(newdir)

if __name__== '__main__':
    setup()
      